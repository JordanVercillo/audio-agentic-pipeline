# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ipynb_checkpoints/

# ── Secrets ──
.env
.cache                 # spotipy token cache (legacy default location)
.spotify_cache         # legacy spotify/ OAuth cache
.spotify_pkce_cache    # PKCE token cache (written under data/)

# ── Data (rebuildable via scripts/run_pipeline.py — never commit) ──
# Whole tree: warehouse layers, raw audio, AND the PKCE token cache live here.
data/
*.parquet
*.mp3

# ── IDE / tool state ──
anaconda_projects/
.vscode/
.idea/

# ── Generated outputs ──
*.faiss
temporal_analysis.html
temporal_analysis.pdf

# ── Runtime logs (scheduled-task output, SELF_HOSTING §4) ──
logs/

# ── Cache backups (contain the db + spectrograms — SELF_HOSTING §5) ──
backups/

# D-21: course-material KB is local-only — excluded from the public repo (P3.7 scrub)
llm_knowledge_base/
