# audio-agentic-pipeline — Python Dependencies
# ==============================================
# NOTE: pyproject.toml + uv.lock are the canonical, pinned source of truth
# (SPEC P6). This file is kept as a pip-compatible export for anyone not
# using uv:  uv pip install -r requirements.txt  (or plain pip).

# ── Core DSP & Audio I/O ──
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0

# ── Data Serialization (Parquet — NEVER CSV for feature matrices) ──
pandas>=2.0.0
pyarrow>=14.0.0

# ── Spotify API ──
spotipy>=2.23.0

# ── Audio Acquisition (YouTube → MP3, Phase 1) ──
# Minimum version enforced for CVE patches (command injection + RCE).
# See CLAUDE_INSTRUCTIONS.md — Security Requirements.
yt-dlp>=2026.2.21

# ── Vector Similarity Search ──
faiss-cpu>=1.7.0

# ── Distributed Processing (PySpark for batch transforms) ──
pyspark>=3.5.0

# ── Dimensionality Reduction (UMAP for taste maps) ──
umap-learn>=0.5.0

# ── Clustering (taste-map KMeans + silhouette; SPEC P1) ──
scikit-learn>=1.3.0

# ── Visualization ──
matplotlib>=3.7.0

# ── HTML report templating (SPEC P4) ──
jinja2>=3.1.0

# ── Agent Access Layer: MCP server + read-only SQL (SPEC P5) ──
mcp>=1.2.0
duckdb>=1.1.0

# ── Deep Audio Embeddings (Optional — pipeline works without it) ──
# Uncomment to enable PANNs 2048D embeddings:
# panns-inference>=0.1.0

# ── .env credential loading (required — auth.py has no in-code fallbacks) ──
python-dotenv>=1.0.0
